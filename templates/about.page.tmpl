{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is About Page</h1>
                <p>About Khanh Nguyen</p>
                <p>This came from the template: {{index .StringMap "test"}}</p>
                
                <p>
                    {{if ne (index .StringMap "remote_ip") ""}}
                        Your remote IP address: {{index .StringMap "remote_ip"}}
                    {{else}}
                        Don't know your IP address. Visit <a href="/" target="_blank">HomePage</a>
                    {{end}}

                </p>
            </div>
            
        </div>
        
    </div>

{{end}}
